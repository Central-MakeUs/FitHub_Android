<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="android:windowSplashScreenBackground">@color/gray_900</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/logo_fithub_bit</item>
        <item name="postSplashScreenTheme">@style/Theme.FitHub_Android</item>

        <!-- 필요시 다크모드 같은 상황 고려. Status bar and Nav bar configs -->
        <item name="android:statusBarColor">@color/gray_900</item>
        <item name="android:navigationBarColor">@color/gray_900</item>
    </style>
</resources>